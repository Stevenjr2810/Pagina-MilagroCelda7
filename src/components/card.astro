---
const { image, title, description } = Astro.props;
---

<div class="w-full max-w-md bg-white border border-gray-200 rounded-lg shadow-sm dark:bg-gray-800 dark:border-gray-700">
  <img class="rounded-t-lg w-full h-72 object-cover" src={image} alt={title} />
  <div class="p-6">
    <h5 class="mb-3 text-2xl font-bold tracking-tight text-gray-900 dark:text-white">{title}</h5>
    <p class="text-base text-gray-700 dark:text-gray-300">{description}</p>
  </div>
</div>
